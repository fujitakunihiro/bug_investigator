# 不具合捜査ツール 開発ガイド

## 目的

正常ログと異常ログを比較し、正常系から最初に外れた地点（FIRST DIVERGENCE）を調査起点として提示する Windows 向けデスクトップツールを開発する。原因を断定する機能ではなく、調査対象を明確にすることを目的とする。

## 適用範囲

このリポジトリ全体に適用する。既存ログフィルタとは別プロジェクトとして扱い、既存コードは必要な場合だけ参考または限定的に再利用する。既存ログフィルタの UI や設計をそのまま継承しない。

## 技術・制約

- Python 3、標準ライブラリのみを使用する。`pip install`、外部 API、外部 LLM、ネットワーク送信は禁止。
- GUI は `tkinter` を使用する。
- Windows 環境で動作することを前提とする。
- 元ログは読み取り専用で扱い、絶対に変更しない。
- 大きなテキストログと文字コードエラーを想定し、1ファイルの読み込み失敗でアプリ全体を停止させない。

## アーキテクチャ

解析ロジックを UI に記述しない。責務は次のように分離する。

- `src/core/log_loader.py`: ログの読み込み、エラーの扱い
- `src/core/normalizer.py`: Timestamp、16進アドレス、任意正規表現ルールの正規化
- `src/core/diff_engine.py`: 正規化済みログの Missing / Added / Changed 比較
- `src/core/divergence.py`: 比較結果から最初の差分と前後コンテキストを決定
- `src/ui/main_window.py`: ファイル選択、開始操作、結果表示のみ
- `src/config/normalization.json`: UIや比較処理から独立した正規化ルール
- `tests/`: core 各責務の自動テスト

## V1 の優先順位

正常ログ1本と異常ログ1本を指定し、読み込み、正規化、比較、FIRST DIVERGENCE の検出、前後ログ（目安20行）の表示までを完成させる。最初の差分は他の差分より明確に強調する。表示量を増やすことより、調査起点の確認しやすさを優先する。

V1では、複数正常ログ、Timing / Order / Value 異常、Repeat、正常出現率、ランキング、AI解析、機械学習、高度なグラフ表示を実装しない。

## 実装順序と検証

1. 小さな正常・異常テストログを作成する。
2. `LogLoader` を実装し、元ログ非変更と読み込みエラー処理を確認する。
3. `Normalizer` を実装し、異なる Timestamp が同一イベントになることを確認する。
4. `DiffEngine` を実装し、Missing / Added / Changed を確認する。
5. `FIRST DIVERGENCE` 判定と前後コンテキストを実装する。
6. Unit Test を追加・実行する。
7. tkinter UI を実装する。
8. 実ログで確認し、必要な正規化ルールだけ追加する。

各段階で動作確認を行い、前段の確認が済むまで次段の機能に進まない。最初の完成目標は、ClockReady が異常ログから欠落したテストで `MISSING / ClockReady` を自動判定・表示できることとする。

## テスト方針

- 正規化、差分分類、FIRST DIVERGENCE を UI から独立してテストする。
- 最低限、Timestamp が異なる同一イベントと、提示された ClockReady Missing ケースをカバーする。
- 解析結果には、差分種別、期待される行、直前行、直後行を検証可能な形で保持する。
